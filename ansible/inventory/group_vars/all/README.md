# Group Vars (Examples)
These files are examples only. Copy and edit as needed.

- `vault.yml.example` — secret placeholders
- `power_profiles.yml.example` — power profile groups
- `power_tags.yml.example` — tag map for power profiles
