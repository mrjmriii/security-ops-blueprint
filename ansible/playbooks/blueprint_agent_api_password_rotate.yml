---
- name: Telemetry manager API credential rotation - blueprint
  hosts: siem
  gather_facts: false
  vars:
    telemetry_api_user: "{{ telemetry_api_user | default('telemetry-api') }}"
    telemetry_api_password: "{{ telemetry_api_password | default('REPLACE_ME') }}"

  tasks:
    - name: Describe API credential rotation flow (placeholder)
      ansible.builtin.debug:
        msg: >-
          Rotate API credentials for {{ telemetry_api_user }} and update
          downstream integrations with the new secret.
